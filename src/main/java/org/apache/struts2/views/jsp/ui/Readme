覆盖jsp中struts2的<s:a></s:a>标签的实现类
因为在web工程中，classes文件夹的加载 优先于jar包的加载
所以只要有两个同样的.class文件，会先优先加载classes文件夹中的。
这样就实现了 修改源码的手法

所以在某些地方用
<a></a>----->用在必要的功能上
有些地方用
<s:a></s:a>->用在需要权限的功能上
就体现出来了
